# 忽略所有隐藏文件和目录
.*
# 但不忽略特定隐藏文件和目录
!.env.example
!.gitignore
!.github/

# 敏感文件
*.pem
*_key
*_secret
*.key
*.crt

# IDE和编辑器文件
*.swp
*.swo

# 构建和依赖文件
node_modules/
/dist/
/build/
*.log

# 临时文件
tmp/
temp/
logs/
*.pid

WeKnora
WeKnora-lite*
cmd/desktop/build/bin/
/models/
test/data/mswag.txt
data/files/
data/weknora.db
data/weknora.db-wal
data/weknora.db-shm
skills-lock.json

web/

**/__pycache__
/scripts/scale_dev_jobs.sh
/server
frontend/.vite
frontend/chrome-extension/
WeKnora-Chrome-Extension

# NAS 部署专用:本地 Windows 编译产物 / 打包归档
WeKnora-lite.exe
weknora-source.tar.gz

# Python 包构建产物(docreader 是 Python 包,pip install -e . 会在此生成)
docreader/docreader.egg-info/

# 跟 WeKnora 无关的整库 clone(本地为方便调 ODL hybrid 而拉过来)
odl-src/

# 本机一次性 probe 脚本 + Windows 环境管理脚本(不属 WeKnora 仓库)
tmp-*.py
*.ps1

# packages/ 里下载到 NAS 的二进制(protoc / grpc_health_probe,目录本身保留以便 .gitkeep 占位)
packages/protoc-*.zip
packages/grpc_health_probe-linux-*

Claude.md

# 本地测试脚本
configure-ollama.sh
fix-docker.sh

# WeChat Mini Program local/private configuration
miniprogram/project.private.config.json
